
TO-DO LIST:

    -HANDLE STARTING TILES = NO CHANGE THEM
    -ACTUALLY WRITE ALGORITHMS KEKW
        -GENERATING BOARDS
        -CHECKING IF EQUAL
    -PROPERLY SET SOLUTION AND STARTING BOARD VALUES
    -CREATE SUBMIT BUTTON
        -CREATE SUCCESS PAGE
        -OPTION TO HIGHLIGHT SPACES THAT GOT WRONG
        -SHOW ERROR # AND ^

